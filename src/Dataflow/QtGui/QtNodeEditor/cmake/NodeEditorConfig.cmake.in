get_filename_component(NodeEditor_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET NodeEditor::NodeEditor)
    include(${CMAKE_CURRENT_LIST_DIR}/../../QtFunctions.cmake)
    check_and_set_qt_version("@QT_DEFAULT_MAJOR_VERSION@")
    find_qt_dependency(COMPONENTS Core Widgets OpenGL Gui REQUIRED)

    include("${NodeEditor_CMAKE_DIR}/NodeEditorTargets.cmake")
endif()

set(NodeEditor_LIBRARIES NodeEditor::NodeEditor)
